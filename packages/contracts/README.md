# Contracts Package

The contracts package exposes implementation helpers for repository-backed schemas and fixtures.

## Product Core Contracts

Product Core contracts map runtime model ids to schema and fixture paths. They do not replace JSON schemas;
they make the schema-backed contracts easier for app and package code to reference.
